{
  "name": "deno-kaboom-template",
  "description": "An example app created with Deno",
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle source/main.js --outfile=www/game.js",
    "dev": "esbuild --bundle source/main.js --outfile=www/game.js --servedir=./www"
  },
  "dependencies": {
    "esbuild": "^0.20.2",
    "kaplay": "3001.0.0-alpha.10"
  }
}